# ─────────────────────────────────────────────────────────────────
# Managed by Cosmos-SincoERP/.github — do not edit manually in this repo.
# Source: docs/templates/dependabot-docker.yml in Cosmos-SincoERP/.github
# Cambios locales se sobrescriben por el sync workflow.
# Para personalizar: editar el template fuente o añadir override al manifest.
# ─────────────────────────────────────────────────────────────────
version: 2
updates:
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "wednesday"
    open-pull-requests-limit: 5
    groups:
      all:
        applies-to: version-updates
        patterns: ["*"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "friday"
    open-pull-requests-limit: 5
    groups:
      all:
        applies-to: version-updates
        patterns: ["*"]
