// Plantilla de configuración runtime del SPA. El reusable deploy-front extrae los
// placeholders ${VAR_NAME}, los resuelve desde Key Vault por convención
// (${API_BASE_URL} con app_name={{BC_KEY}} → secret front-{{BC_KEY}}-api-base-url)
// y genera el env.js servido junto al bundle.
//
// TODO({{REPO_NAME}}): ajustar las variables que la app necesita y sembrar sus
// secrets en el Key Vault del BC. Escape hatch: .deploy/env.map para nombres custom.
window.__APP_CONFIG__ = {
  apiBaseUrl: "${API_BASE_URL}",
};
